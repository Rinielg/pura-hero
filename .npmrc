# @react-three/fiber declares optional Expo peers that npm tries to resolve
# against React 19 and fails on. Every install needs this — including the one
# Vercel runs, which has no shell for us to pass a flag to.
legacy-peer-deps=true
